<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="#949393">



    <!--Button
        android:id="@+id/button"
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:layout_marginBottom="16dp"
        android:text="Go To Login Activity"
        app:layout_constraintBottom_toTopOf="@+id/btnMenu"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" /-->

    <ImageView
        android:id="@+id/ivMain"
        android:layout_width="wrap_content"
        android:layout_height="400dp"
        android:layout_marginBottom="32dp"
        android:contentDescription="@string/ivMain"
        android:src="@drawable/virus_main"

        android:outlineAmbientShadowColor="#cecece"
        app:layout_constraintBottom_toTopOf="@+id/btnLeaderboard"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btnLeaderboard"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:textSize="12dp"
        android:text="@string/btnLeaderboard"
        android:textColor="#7c00ff"
        android:layout_marginTop="520dp"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/btnFindCure"
        app:layout_constraintEnd_toStartOf="@+id/btnEndless" />

    <Button
        android:id="@+id/btnEndless"
        android:layout_width="110dp"
        android:layout_height="60dp"
        android:text="@string/btnEndless"
        android:textColor="#7c00ff"
        android:layout_marginTop="520dp"
        android:layout_marginEnd="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <Button
        android:id="@+id/btnFindCure"
        android:layout_width="110dp"
        android:text="@string/btnFindCure"
        android:layout_height="60dp"
        android:textColor="#7c00ff"
        android:layout_marginStart="32dp"
        android:layout_marginTop="520dp"

        app:layout_constraintEnd_toStartOf="@+id/btnLeaderboard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>